rootProject.name = 'notification-server'

include (
        'apps:api',
        'apps:publish',
        'apps:consumer',
        'apps:enums',
        'storage:db',
        'supplier:common',
        'supplier:direct',
        'supplier:mq',
)
